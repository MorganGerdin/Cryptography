# Returns the smallest number between x and y.
# If both numbers are the same, x is returned.
# Note: you can't use Python's built-in min function :)
def minimum(x, y):
    return 0


def test_min():
    assert minimum(5, 10) == 5
