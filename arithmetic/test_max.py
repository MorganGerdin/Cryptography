# Returns the largest number between x and y.
# If both numbers are the same, x is returned.
# Note: you can't use Python's built-in max function :)
def maximum(x, y):
    return 0


def test_max():
    assert maximum(5, 70) == 70
